{
  "name": "dsh-dba-agent",
  "version": "0.1.0",
  "description": "SQL optimization capability pack for the DeepSeek Harness: a two-level router prompt, one skill, and one MCP-backed MySQL evidence tool.",
  "type": "module",
  "main": "lib/index.js",
  "files": [
    "lib",
    "cordis.patch.yml",
    "presets",
    "scripts",
    "skills"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf lib",
    "test": "node --test tests/*.test.js",
    "check": "npm run build && npx tsc -p tsconfig.json --noEmit --noUnusedLocals --noUnusedParameters && npm test",
    "prepare": "npm run build",
    "install-preset": "bash scripts/install-preset.sh"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  }
}

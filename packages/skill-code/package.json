{
  "name": "dsh-dba-skill-code",
  "version": "0.1.0",
  "description": "DSH plugin: registers the `mysql-query` skill in code via ctx.skills.register() (the runtime-register path), with config->env bridging.",
  "type": "module",
  "main": "lib/index.js",
  "files": [
    "lib",
    "cordis.patch.yml",
    "scripts",
    "skills"
  ],
  "scripts": {
    "sync": "mkdir -p scripts && cp ../../scripts/mysql_query.sh scripts/mysql_query.sh",
    "build": "npm run sync && tsc -p tsconfig.json && npm run gen:patch",
    "gen:patch": "sed \"s#name: dsh-dba-skill-code#name: $PWD/lib/index.js#\" cordis.patch.yml > cordis.patch.local.yml",
    "clean": "rm -rf lib scripts cordis.patch.local.yml"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  }
}
